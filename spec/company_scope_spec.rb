require 'spec_helper'

describe CompanyScope do
  it 'has a version number' do
    expect(CompanyScope::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
